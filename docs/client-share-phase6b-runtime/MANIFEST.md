# Client Share Link -- Phase 6B DB Apply Boundary Runtime Package Manifest

File `00` is mechanically generated by `scripts/client-share/build-phase6b-runtime-prerequisites-package.ps1`;
every other file in this package is hand-authored (small enough that the full
staged multi-file generator discipline used by the Phase 6A package is not
warranted -- see `00_READ_ME_FIRST.md`). Deterministic from repository/package
file contents alone -- no wall-clock timestamp is embedded anywhere in this file.
Re-run the generator to reproduce it exactly, or to pick up a source change.

## Package files

| # | File | Origin | SHA-256 |
|---|---|---|---|
| 1 | `00_READ_ME_FIRST.md` | hand-authored | `69ad9312c228befbca53ebe8a99c54915bb7e33fd6b19d5300792be4a1dd8e47` |
| 2 | `00_APPLY_CURRENT_PROJECT_UPDATE_APPLY_PREREQUISITES.sql` | **generated** (mechanically assembled from the one source migration below) | `138dd19b7fd71afaf32d2d2edac0a49abe699d5ca1880fa3ec066996841d8843` |
| 3 | `01_RUN_PHASE6B_BOUNDARY_RUNTIME_TESTS.sql` | hand-authored | `dabd05268cc8d0272385faa286ef1093dddbde01ef974b7b5e379977dc0a658b` |
| 4 | `02_CAPTURE_RESULTS.md` | hand-authored | `33a09181a13dcc013232a525a2862e77c0c4771d2eea196a69e0b7efa9497d10` |
| 5 | `MANIFEST.md` | **generated** (this file) | *(intentionally not embedded -- self-referential fixed point)* |
| 6 | `build-phase6b-runtime-prerequisites-package.ps1` | hand-authored (this generator) | `03780904ed32d6f3572b3cf592724da207f8eb4c2d8c1cd453449eec8caa807e` |

## Migration this package exercises

Applied by file `00` verbatim THROUGH its own trailing "One-time historical
backfill" statement, which is deliberately omitted (see file `00`'s own header
and the generator's truncation-logic comment for the full proof this is safe --
in short: that statement is a directly-executed top-level DML referencing a
column the disposable fixture does not carry, is unrelated to any function/
trigger/grant this package installs, and is a one-time PRODUCTION data fixup
that is a no-op against any project with no matching historical data). The
three CREATE OR REPLACE FUNCTION statements themselves are 100% verbatim,
character-for-character, with zero hand-editing. See that generator's own
header comment for the full dependency analysis of why this ONE migration (of
the five that have ever redefined `apply_project_update_transaction`) is
sufficient.

| Migration | SHA-256 (full file) |
|---|---|
| `supabase/migrations/202607270001_project_completion_reconciliation.sql` | `8d22bc16c851ea7f53b2c4e7af92443317043df377bf48073b171e98a5dbed9a` |

The Phase 6B DB apply boundary migration this whole package is verifying --
`supabase/migrations/202608230001_client_share_apply_boundary.sql` -- is applied
directly from its own real location, not duplicated into this package (see
`00_READ_ME_FIRST.md`), so it is not listed as a source migration here.

## To reproduce or re-verify these hashes

```
powershell -File scripts/client-share/build-phase6b-runtime-prerequisites-package.ps1
```

## Confirmations

- The generator is read-only against `supabase/migrations/**`.
- The generated output is built and fully validated in a temporary staging file
  BEFORE the real package file is touched.
- This package targets the SAME disposable Supabase project the Phase 6A runtime
  package already used -- it does not provision or describe a new one, and does
  not modify any file under `docs/client-share-phase6a-runtime/`.
- No Production project URL, project reference, credential or environment value
  appears anywhere in this package.
- This file embeds no wall-clock timestamp; re-running the generator against
  unchanged inputs reproduces it byte-for-byte.
